<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="DiscouragedApi,LockedOrientationActivity">
        <activity android:name=".activities.Settings"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.MultiplayerMenu"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.Normal"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.GameFinished"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.Join"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.Waiting"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.Create"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.Multiplayer"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.MainActivity"
            android:screenOrientation="portrait"
                android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>